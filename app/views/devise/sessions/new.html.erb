<%# \\wsl.localhost\Ubuntu\home\aivets\projects\worryaway\app\views\devise\sessions\new.html.erb %>

<div class="home-body">
  <h2 class="centered-text">ユーザーログイン</h2>

  <%= form_with model: resource, as: resource_name, url: session_path(resource_name), local: true do |f| %>
    <div class="field">
      <%= f.label :メールアドレス　 %><br />
      <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "xxx@xxx.com" %>
    </div>

    <div class="field">
      <%= f.label :パスワード %><br />
      <%= f.password_field :password, autocomplete: "current-password", placeholder: "xxxxxxxx" %>
    </div>

    <%# <% if devise_mapping.rememberable? %>
      <%# <div class="field"> %>
        <%# <%= f.check_box :remember_me %>
        <%# <%= f.label :remember_me %>
      <%# </div> %>
    <%# <% end %>

    <div class="actions centered-text">
      <%= link_to "トップページに戻る", root_path, class: "btn-standard1" %>
      <%= f.submit "ログイン", class: "btn-standard2" %>
    </div>
  <% end %>
</div>